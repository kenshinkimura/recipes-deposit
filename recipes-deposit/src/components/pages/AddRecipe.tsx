import { Link } from 'react-router-dom';

export const AddRecipe: React.FC = () => {
    return (
        <div>
            <div>
                <Link to="/">go to home page</Link></div>
        </div>
    );
};
